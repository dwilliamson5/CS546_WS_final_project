<div class="container">
    {{#if user}}
        <p>Welcome to Unisell!</p>

        <p>Browsing at {{universityName}}</p>

        Take a look at today's items: (show items here w/ search)

        <a href="/auth/logout" class="btn btn-danger">Logout</a>
        
        {{#if super_admin}}
            <a href="/admin" class="btn btn-warning">Admin Panel</a>
        {{/if}}  
    {{else}}
        <p>Welcome to Unisell! This is the about page.</p>

        <a href="/auth/login" class="btn btn-success">Login</a>
        <a href="/auth/signup" class="btn btn-primary">Create an account</a>
    {{/if}}
</div>
