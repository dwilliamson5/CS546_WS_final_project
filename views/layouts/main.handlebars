<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/public/css/main.css">
</head>

<body>
    <main>
        <header class="p-3 bg-dark text-white">
            <div class="container">
                <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                    <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
                        <img class="logo" src="/public/images/logo.png" alt="Unisell">
                    </a>

                    <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
                    </ul>

                    <div class="text-end">
                        {{#if user}}
                            <a href="/items/new" class="btn btn-success">List New Item</a>

                            {{#if superAdmin}}
                                {{#if inAdmin}}
                                    <a href="/" class="btn btn-danger">Exit Admin</a>
                                {{else}}
                                    <a href="/admin" class="btn btn-outline-warning">Admin Panel</a>
                                {{/if}}
                            {{/if}}
                            <a href="/profile/edit" class="btn btn-outline-light">Edit Profile</a>
                            <a href="/auth/logout" class="btn btn-danger">Logout</a>
                        {{else}}
                            <a href="/auth/login" class="btn btn-success">Login</a>
                            <a href="/auth/signup" class="btn btn-primary">Create an account</a>
                        {{/if}}
                    </div>
                </div>
            </div>
        </header>

        <div class="container">
          <br>
          <h1 class="title">{{ title }}</h1>

          {{{body}}}
        </div>

        <footer>
        </footer>
    </main>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://code.jquery.com/jquery-2.2.4.min.js"
        integrity="sha256-BbhdlvQf/xTY9gja0Dq3HiwQF8LaCRTXxZKRutelT44=" crossorigin="anonymous"></script>
    <script src="/public/js/main.js"></script>
</body>

</html>
